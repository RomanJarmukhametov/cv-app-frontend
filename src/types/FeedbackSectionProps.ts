/* This TypeScript code defines an interface named `FeedbackSectionProps`. An interface in TypeScript
is a way to define the shape of an object. In this case, the `FeedbackSectionProps` interface
specifies that any object implementing this interface must have the following properties: */
interface FeedbackSectionProps {
  id: number;
  __component: string;
  title: string;
  sectionId: string;
  description: string;
  buttonText: string;
}

export default FeedbackSectionProps;
